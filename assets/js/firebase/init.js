import { initializeApp } from 'https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js';
import { getFirestore } from 'https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js';
import { getAuth, signInWithEmailAndPassword } from 'https://www.gstatic.com/firebasejs/10.7.1/firebase-auth.js';

// Função para obter credenciais (lendo de variáveis de ambiente)
async function getFirebaseCredentials() {
    // Sempre tentar ler credenciais das variáveis de ambiente primeiro (GitHub Pages)
    const email = window.FIREBASE_AUTH_EMAIL || process.env.FIREBASE_AUTH_EMAIL;
    const password = window.FIREBASE_AUTH_PASSWORD || process.env.FIREBASE_AUTH_PASSWORD;
    
    if (email && password) {
        console.log('✅ Credenciais carregadas das variáveis de ambiente');
        return { email, password };
    }
    
    // Fallback: tentar servidor local apenas se não conseguir ler das variáveis de ambiente
    try {
        const response = await fetch('/api/firebase-auth');
        if (response.ok) {
            const credentials = await response.json();
            console.log('🔐 Credenciais carregadas do servidor local (fallback)');
            return credentials;
        }
    } catch (error) {
        console.warn('⚠️ Servidor local não disponível');
    }
    
    // Sem fallback - deve falhar se não conseguir carregar
    console.error('❌ Credenciais Firebase não encontradas');
    throw new Error('FIREBASE_AUTH_EMAIL e FIREBASE_AUTH_PASSWORD devem estar configuradas');
}

export async function loadFirebaseConfig() {
    const version = (typeof window !== 'undefined' && window.VERSION) ? `?v=${window.VERSION}` : '';
    
    // Carregar configuração do GitHub Pages
    try {
        const resp = await fetch(`./firebase-config.public.js${version}`);
        if (resp.ok) {
            const script = await resp.text();
            eval(script);
            
            // Verificar se os valores são placeholders
            if (window.FIREBASE_API_KEY === "PLACEHOLDER_API_KEY" || 
                window.FIREBASE_AUTH_DOMAIN === "PLACEHOLDER_AUTH_DOMAIN") {
                console.error('❌ ERRO: Configuração Firebase não foi gerada pelo GitHub Actions');
                console.error('❌ Configure os secrets no GitHub e faça push para a branch main');
                throw new Error('Firebase configuration not generated by GitHub Actions');
            }
            
            if (window.FIREBASE_API_KEY && window.FIREBASE_PROJECT_ID) {
                return {
                    apiKey: window.FIREBASE_API_KEY,
                    authDomain: window.FIREBASE_AUTH_DOMAIN,
                    projectId: window.FIREBASE_PROJECT_ID,
                    storageBucket: window.FIREBASE_STORAGE_BUCKET,
                    messagingSenderId: window.FIREBASE_MESSAGING_SENDER_ID,
                    appId: window.FIREBASE_APP_ID
                };
            }
        }
    } catch (error) {
        console.error('❌ Erro ao carregar configuração Firebase:', error);
        throw error;
    }
    
    // Não há fallback - deve falhar se não conseguir carregar
    throw new Error('Failed to load Firebase configuration');
}

// Singleton para evitar múltiplas inicializações
let firebaseInstance = null;
let initPromise = null;

export async function initFirebase() {
    // Se já foi inicializado, retornar a instância existente
    if (firebaseInstance) {
        // console.log('🔄 Firebase já inicializado, reutilizando instância');
        return firebaseInstance;
    }
    
    // Se já está sendo inicializado, aguardar a promessa existente
    if (initPromise) {
        // console.log('⏳ Firebase já está sendo inicializado, aguardando...');
        return await initPromise;
    }
    
    // Inicializar Firebase pela primeira vez
    console.log('🚀 Inicializando Firebase pela primeira vez...');
    initPromise = _initFirebase();
    
    try {
        firebaseInstance = await initPromise;
        return firebaseInstance;
    } finally {
        initPromise = null;
    }
}

async function _initFirebase() {
    const config = await loadFirebaseConfig();
    const app = initializeApp(config);
    const db = getFirestore(app);
    const auth = getAuth(app);

    try {
        // Obter credenciais do servidor local
        const credentials = await getFirebaseCredentials();
        
        // Tenta fazer login com o usuário da aplicação
        // console.log('🔐 Tentando autenticar usuário da aplicação...');
        await signInWithEmailAndPassword(auth, credentials.email, credentials.password);
        console.log('✅ Login do usuário da aplicação no Firebase bem-sucedido!');
    } catch (error) {
        console.error('❌ Erro ao fazer login do usuário da aplicação no Firebase:', error.message);
        console.warn('⚠️ Operações de escrita no Firestore podem falhar sem autenticação');
    }

    return { app, db, auth };
}


